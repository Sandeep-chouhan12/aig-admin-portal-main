import { PdfGenertoreClass } from './pdf-genertore-class';

describe('PdfGenertoreClass', () => {
  it('should create an instance', () => {
    expect(new PdfGenertoreClass()).toBeTruthy();
  });
});
