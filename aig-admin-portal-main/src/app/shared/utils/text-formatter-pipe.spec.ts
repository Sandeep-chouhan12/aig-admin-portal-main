import { TextFormatterPipe } from './text-formatter-pipe';

describe('TextFormatterPipe', () => {
  it('should create an instance', () => {
    expect(new TextFormatterPipe()).toBeTruthy();
  });
});
