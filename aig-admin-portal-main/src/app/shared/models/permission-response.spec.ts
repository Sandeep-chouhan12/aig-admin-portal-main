import { PermissionResponse } from './permission-response';

describe('PermissionResponse', () => {
  it('should create an instance', () => {
    expect(new PermissionResponse()).toBeTruthy();
  });
});
