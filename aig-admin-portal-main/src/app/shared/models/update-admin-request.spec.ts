import { UpdateAdminRequest } from './update-admin-request';

describe('UpdateAdminRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateAdminRequest()).toBeTruthy();
  });
});
