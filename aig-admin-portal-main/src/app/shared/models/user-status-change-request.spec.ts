import { UserStatusChangeRequest } from './user-status-change-request';

describe('UserStatusChangeRequest', () => {
  it('should create an instance', () => {
    expect(new UserStatusChangeRequest()).toBeTruthy();
  });
});
