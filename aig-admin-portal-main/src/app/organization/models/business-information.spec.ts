import { BusinessInformation } from './business-information';

describe('BusinessInformation', () => {
  it('should create an instance', () => {
    expect(new BusinessInformation()).toBeTruthy();
  });
});
