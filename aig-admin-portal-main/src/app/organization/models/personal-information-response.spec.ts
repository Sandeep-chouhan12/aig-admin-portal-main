import { PersonalInformationResponse } from './personal-information-response';

describe('PersonalInformationResponse', () => {
  it('should create an instance', () => {
    expect(new PersonalInformationResponse()).toBeTruthy();
  });
});
