import { DirectorInformation } from './director-information';

describe('DirectorInformation', () => {
  it('should create an instance', () => {
    expect(new DirectorInformation()).toBeTruthy();
  });
});
