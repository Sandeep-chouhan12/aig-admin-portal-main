import { EmergencyOfficerRequest } from './emergency-officer-request';

describe('EmergencyOfficerRequest', () => {
  it('should create an instance', () => {
    expect(new EmergencyOfficerRequest()).toBeTruthy();
  });
});
