import { AddressVerificationDetails } from './address-verification-details';

describe('AddressVerificationDetails', () => {
  it('should create an instance', () => {
    expect(new AddressVerificationDetails()).toBeTruthy();
  });
});
