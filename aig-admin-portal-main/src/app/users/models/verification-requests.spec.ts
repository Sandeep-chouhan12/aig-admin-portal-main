import { VerificationRequests } from './verification-requests';

describe('VerificationRequests', () => {
  it('should create an instance', () => {
    expect(new VerificationRequests()).toBeTruthy();
  });
});
