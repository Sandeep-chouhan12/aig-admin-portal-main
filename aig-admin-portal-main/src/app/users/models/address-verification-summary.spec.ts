import { AddressVerificationSummary } from './address-verification-summary';

describe('AddressVerificationSummary', () => {
  it('should create an instance', () => {
    expect(new AddressVerificationSummary()).toBeTruthy();
  });
});
